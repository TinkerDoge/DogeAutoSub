# DogeAutoSub
1: You need to maually create the virtualenv named venv
2: after created the venv activate the venv
3: then do pip install -r requirements.txt
4: you will need to download ffmpeg packages and copy the ffmpeg folder to /venv/Scripts/
5: after that, you can launch the app with the DogeAutoSub.bat file
