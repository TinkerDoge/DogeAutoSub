# DogeAutoSub
You need to maually create the virtualenv named venv
after created the venv activate the venv
then do pip install -r requirements.txt
you will need to download ffmpeg packages and copy the ffmpeg folder to /venv/Scripts/
after that, you can launch the app with the DogeAutoSub.bat file
